# The fixture field is only required for running tests
fixture = test
# Testing is done on an in-memory H2 DB.
db = h2

h2 = {
  url = "jdbc:h2:mem:myDb;DB_CLOSE_DELAY=-1"
  driver = org.h2.Driver
  user = "root"
  password = "root"
}

# Minimal configurations that are required
model.cache.size = 3
request.bytes.size = 104857600